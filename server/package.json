{
  "name": "treino-servidor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API do guia de treino. Sem dependencias: usa node:http e node:sqlite.",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "criar-usuario": "node scripts/criar-usuario.js",
    "test": "node --test test/*.test.js"
  }
}
